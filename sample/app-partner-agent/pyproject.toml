[project]
name = "app-partner-agent"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "agent-framework",
    "agent-framework-openai",
    "azure-identity",
    "azure-servicebus",
    "azure-storage-blob",
    "python-dotenv",
]

[tool.uv]
package = false
